---
import Cloudflare from "@/components/head/Cloudflare.astro";
import SEO from "@/components/head/SEO.astro";
import SkipLink from "@/components/head/SkipLink.astro";
import ThemeToggle from "@/components/head/ThemeToggle.astro";

import Footer1, { type SocialLink } from "@/components/starwind-pro/footer-01/Footer1.astro";
import Navbar1 from "@/components/starwind-pro/navbar-01/Navbar1.astro";

import { site } from "@/content/site";
import "@/styles/global.css";
import { getIcon } from "@/utils/getIcon";

const {
  title = site.name,
  description = site.description,
  canonicalPath = Astro.url.pathname,
  ogImage = site.seo.ogImage,
  robots = site.seo.robots,
  jsonLd,
  contentCentered = false,
} = Astro.props;

const CF_TOKEN = "XXX";
const canonical = new URL(canonicalPath, site.url).toString();

const socialLinks: SocialLink[] =
  site.author.socials?.map((social) => ({
    name: social.label,
    href: social.href,
    icon: getIcon(social.label),
  })) ?? [];
---

<!doctype html>
<html lang={site.locale}>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <meta name="theme-color" content={site.seo.themeColor} />

    <SEO
      title={title}
      description={description}
      canonical={canonical}
      ogImage={ogImage}
      robots={robots}
      jsonLd={jsonLd}
    />
    <ThemeToggle />
    <Cloudflare token={CF_TOKEN} />
  </head>

  <body class="bg-background text-foreground flex min-h-dvh flex-col scheme-light dark:scheme-dark">
    <SkipLink />
    <Navbar1 siteName={site.name} links={site.nav} ctaText="Get Started" ctaHref="#get-started" />
    <main
      id="main"
      class:list={[
        "mx-auto w-full flex-1 shrink-0 basis-auto px-1 py-8 sm:px-6",
        contentCentered && "grid place-items-center",
      ]}
    >
      <slot />
    </main>
    <Footer1 socialLinks={socialLinks} companyName={site.name} />
  </body>
</html>
